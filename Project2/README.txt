To run the program:

cd into the folder containing project2.c, project2.h, student2.c and the makefile
run "make"
run ./p2 [number of messages] [packet loss probability] [corruption probability] [] 
    [average time between messages] [randomization or not] [bi-directional or not] 
	into command line, or fill in prompts as they pop up


